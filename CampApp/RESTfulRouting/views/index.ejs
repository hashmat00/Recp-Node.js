<h1>Blogs Listing</h1>


<% blogs.forEach(function(blog){ %>
    
    <div>
        <h2><%= blog.name %></h2>
        <img src="<%= blog.image %>">
        <span><%= blog.created %></span>
        <p><%= blog.body %></p>
    </div>
    
<% }); %>